const statusCode = require('../../../modules/statusCode');

module.exports = {
  getEachContent: async (req, res) => {
    try {

    } catch (error) {
      console.log(error);
      return res.json(
        error
      )
    }
  }
}